import React from 'react'
import PropTypes from 'prop-types'

const RightSquareComponent = ({children}) => {
    return (
        <div>
           {children} 
        </div>
    )
}

RightSquareComponent.propTypes = {

}

export default RightSquareComponent
