import React from 'react'

const TextChange = (name, position) => {
    return (
        <div>
           <span>{name}</span> 
           <span className="">{position}</span>
        </div>
    )
}

export default TextChange
